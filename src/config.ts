import path from 'path';

export const STATIC_PATH: string = path.join(__dirname, '..', 'public');
export const HTML_FILES_PATH: string = path.join(STATIC_PATH, 'html');

export const PORT: number = 3002;
